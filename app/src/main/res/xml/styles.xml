<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="android:actionBarStyle">@style/MyActionBarStyle</item>
        <!-- For older devices that don't use the "android:" prefix -->
        <item name="actionBarStyle">@style/MyActionBarStyle</item>
    </style>

    <style name="MyActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/customActionBarColor</item>
        <!-- For older devices that don't use the "android:" prefix -->
        <item name="background">@color/customActionBarColor</item>
    </style>

</PreferenceScreen>